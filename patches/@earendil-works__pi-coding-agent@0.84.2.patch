diff --git a/dist/core/agent-session.js b/dist/core/agent-session.js
index 4bcf0d4..748ec14 100644
--- a/dist/core/agent-session.js
+++ b/dist/core/agent-session.js
@@ -97,6 +97,8 @@ export class AgentSession {
     _compactionAbortController = undefined;
     _autoCompactionAbortController = undefined;
     _overflowRecoveryAttempted = false;
+    /** Set when a mid-turn message_end crossed the percentage threshold and aborted the run. */
+    _thresholdAbortPending = false;
     // Branch summarization state
     _branchSummaryAbortController = undefined;
     // Retry state
@@ -396,8 +398,42 @@ export class AgentSession {
                     this._retryAttempt = 0;
                 }
             }
+            // pi-c2 patch: strict percentage enforcement. After EVERY complete
+            // message (any role), check whether context usage reached the
+            // configured percentage threshold. If it did, abort the active turn
+            // immediately so compaction runs without waiting for the turn to
+            // finish. The completed message is always kept whole (never cut off
+            // mid-message) because the check runs on message_end.
+            await this._maybeAbortForThreshold();
         }
     };
+    /**
+     * pi-c2 patch: mid-turn percentage-threshold enforcement.
+     * Called on every message_end. When the session's live context usage
+     * reaches/exceeds the configured percentage of the model's context window,
+     * abort the active agent run. The aborted run is then handled by
+     * _checkCompaction() (which sees _thresholdAbortPending and compacts +
+     * auto-continues). Idempotent: once a threshold abort is pending, further
+     * message_end events do nothing until compaction runs and clears the flag.
+     */
+    async _maybeAbortForThreshold() {
+        if (this._thresholdAbortPending)
+            return;
+        const settings = this.settingsManager.getCompactionSettings();
+        if (!settings.enabled || typeof settings.thresholdPercent !== "number")
+            return;
+        const model = this.model;
+        const contextWindow = model?.contextWindow ?? 0;
+        if (contextWindow <= 0)
+            return;
+        const estimate = estimateContextTokens(this.agent.state.messages);
+        if (estimate.tokens <= 0)
+            return;
+        if (shouldCompact(estimate.tokens, contextWindow, settings)) {
+            this._thresholdAbortPending = true;
+            this.agent.abort();
+        }
+    }
     _willRetryAfterAgentEnd(event) {
         const settings = this.settingsManager.getRetrySettings();
         if (!settings.enabled || this._retryAttempt >= settings.maxRetries) {
@@ -1511,8 +1547,13 @@ export class AgentSession {
         const settings = this.settingsManager.getCompactionSettings();
         if (!settings.enabled)
             return false;
+        // pi-c2 patch: a threshold-triggered abort (mid-turn message_end crossing)
+        // must NOT be skipped like a user cancellation. Clear the flag so the
+        // abort is treated as a compaction trigger below.
+        const thresholdAbort = this._thresholdAbortPending;
+        this._thresholdAbortPending = false;
         // Skip if message was aborted (user cancelled) - unless skipAbortedCheck is false
-        if (skipAbortedCheck && assistantMessage.stopReason === "aborted")
+        if (skipAbortedCheck && assistantMessage.stopReason === "aborted" && !thresholdAbort)
             return false;
         const contextWindow = this.model?.contextWindow ?? 0;
         // Skip overflow check if the message came from a different model.
@@ -1585,7 +1626,9 @@ export class AgentSession {
             contextTokens = directContextTokens;
         }
         if (shouldCompact(contextTokens, contextWindow, settings)) {
-            return await this._runAutoCompaction("threshold", false);
+            // pi-c2 patch: a mid-turn threshold abort resumes the interrupted turn.
+            const willRetry = thresholdAbort;
+            return await this._runAutoCompaction("threshold", willRetry);
         }
         return false;
     }
@@ -1699,7 +1742,10 @@ export class AgentSession {
                 // from agent state. Rebuilding state from the new compaction can restore that kept entry,
                 // leaving an assistant as the final message. agent.continue() rejects that state, so remove
                 // the retriable error or truncated-length response again before continuing the interrupted turn.
-                if (lastMsg?.role === "assistant" && (lastMsg.stopReason === "error" || lastMsg.stopReason === "length")) {
+                // pi-c2 patch: also remove a trailing threshold-aborted assistant so the interrupted
+                // turn can auto-continue after compaction.
+                if (lastMsg?.role === "assistant" &&
+                    (lastMsg.stopReason === "error" || lastMsg.stopReason === "length" || lastMsg.stopReason === "aborted")) {
                     this.agent.state.messages = messages.slice(0, -1);
                 }
                 return true;
@@ -1908,6 +1954,7 @@ export class AgentSession {
                 this._extensionShutdownHandler?.();
             },
             getContextUsage: () => this.getContextUsage(),
+            getSettingsManager: () => this.settingsManager,
             compact: (options) => {
                 void (async () => {
                     try {
diff --git a/dist/core/compaction/compaction.d.ts b/dist/core/compaction/compaction.d.ts
index bf40e20..e084ce1 100644
--- a/dist/core/compaction/compaction.d.ts
+++ b/dist/core/compaction/compaction.d.ts
@@ -29,6 +29,8 @@ export interface CompactionSettings {
     enabled: boolean;
     reserveTokens: number;
     keepRecentTokens: number;
+    /** pi-c2 patch: percentage of the context window that triggers compaction. */
+    thresholdPercent?: number;
 }
 export declare const DEFAULT_COMPACTION_SETTINGS: CompactionSettings;
 /**
diff --git a/dist/core/compaction/compaction.js b/dist/core/compaction/compaction.js
index e7ffee7..deff355 100644
--- a/dist/core/compaction/compaction.js
+++ b/dist/core/compaction/compaction.js
@@ -160,6 +160,13 @@ export function estimateContextTokens(messages) {
 export function shouldCompact(contextTokens, contextWindow, settings) {
     if (!settings.enabled)
         return false;
+    // pi-c2 patch: percentage-based threshold. When the optional
+    // thresholdPercent is configured, compare usage against the configured
+    // percentage of the model's context window instead of the reserve-token
+    // math. Falls back to the stock behavior when absent.
+    if (typeof settings.thresholdPercent === "number" && settings.thresholdPercent > 0) {
+        return contextTokens >= (contextWindow * settings.thresholdPercent) / 100;
+    }
     return contextTokens > contextWindow - settings.reserveTokens;
 }
 // ============================================================================
diff --git a/dist/core/extensions/loader.js b/dist/core/extensions/loader.js
index 324d30a..50a312a 100644
--- a/dist/core/extensions/loader.js
+++ b/dist/core/extensions/loader.js
@@ -157,6 +157,8 @@ export function createExtensionRuntime() {
         setModel: () => Promise.reject(new Error("Extension runtime not initialized")),
         getThinkingLevel: notInitialized,
         setThinkingLevel: notInitialized,
+        // Local experiment: command-only APIs become available after the runner binds.
+        createCommandContext: notInitialized,
         flagValues: new Map(),
         pendingProviderRegistrations: [],
         pendingNativeProviderRegistrations: [],
@@ -316,6 +318,12 @@ function createExtensionAPI(extension, runtime, cwd, eventBus) {
             runtime.assertActive();
             runtime.setThinkingLevel(level);
         },
+        // Local experiment: expose command-context creation to extensions so
+        // lifecycle handlers can obtain an ExtensionCommandContext.
+        createCommandContext() {
+            runtime.assertActive();
+            return runtime.createCommandContext();
+        },
         registerProvider(providerOrName, config) {
             runtime.assertActive();
             if (typeof providerOrName === "string") {
diff --git a/dist/core/extensions/runner.js b/dist/core/extensions/runner.js
index 418865a..8b7a333 100644
--- a/dist/core/extensions/runner.js
+++ b/dist/core/extensions/runner.js
@@ -135,6 +135,7 @@ export class ExtensionRunner {
     abortFn = () => { };
     hasPendingMessagesFn = () => false;
     getContextUsageFn = () => undefined;
+    getSettingsManagerFn = () => undefined;
     compactFn = () => { };
     getSystemPromptFn = () => "";
     getSystemPromptOptionsFn = () => ({ cwd: this.cwd });
@@ -171,6 +172,10 @@ export class ExtensionRunner {
         this.runtime.setModel = actions.setModel;
         this.runtime.getThinkingLevel = actions.getThinkingLevel;
         this.runtime.setThinkingLevel = actions.setThinkingLevel;
+        // Local experiment: let extensions create an ExtensionCommandContext
+        // through the shared runtime (used by lifecycle handlers, not only
+        // command dispatch).
+        this.runtime.createCommandContext = () => this.createCommandContext();
         // Context actions (required)
         this.getModel = contextActions.getModel;
         this.getScopedModels = contextActions.getScopedModels;
@@ -181,6 +186,7 @@ export class ExtensionRunner {
         this.hasPendingMessagesFn = contextActions.hasPendingMessages;
         this.shutdownHandler = contextActions.shutdown;
         this.getContextUsageFn = contextActions.getContextUsage;
+        this.getSettingsManagerFn = contextActions.getSettingsManager;
         this.compactFn = contextActions.compact;
         this.getSystemPromptFn = contextActions.getSystemPrompt;
         this.getSystemPromptOptionsFn = contextActions.getSystemPromptOptions ?? (() => ({ cwd: this.cwd }));
@@ -525,6 +531,10 @@ export class ExtensionRunner {
                 runner.assertActive();
                 return runner.getContextUsageFn();
             },
+            getSettingsManager: () => {
+                runner.assertActive();
+                return runner.getSettingsManagerFn();
+            },
             compact: (options) => {
                 runner.assertActive();
                 runner.compactFn(options);
diff --git a/dist/core/extensions/types.d.ts b/dist/core/extensions/types.d.ts
index 556623b..773d9c3 100644
--- a/dist/core/extensions/types.d.ts
+++ b/dist/core/extensions/types.d.ts
@@ -22,6 +22,7 @@ import type { CustomMessage } from "../messages.ts";
 import type { ModelRegistry } from "../model-registry.ts";
 import type { ScopedModel } from "../model-resolver.ts";
 import type { BranchSummaryEntry, CompactionEntry, CustomEntry, ReadonlySessionManager, SessionEntry, SessionManager } from "../session-manager.ts";
+import type { SettingsManager } from "../settings-manager.ts";
 import type { SlashCommandInfo } from "../slash-commands.ts";
 import type { SourceInfo } from "../source-info.ts";
 import type { BuildSystemPromptOptions } from "../system-prompt.ts";
@@ -242,6 +243,8 @@ export interface ExtensionContext {
     shutdown(): void;
     /** Get current context usage for the active model. */
     getContextUsage(): ContextUsage | undefined;
+    /** pi-c2 patch: the session's settings manager (compaction policy, model defaults). */
+    getSettingsManager(): SettingsManager;
     /** Trigger compaction without awaiting completion. */
     compact(options?: CompactOptions): void;
     /** Get the current effective system prompt. */
@@ -959,6 +962,11 @@ export interface ExtensionAPI {
     getThinkingLevel(): ThinkingLevel;
     /** Set thinking level (clamped to model capabilities). */
     setThinkingLevel(level: ThinkingLevel): void;
+    /**
+     * Create a fresh ExtensionCommandContext bound to the current runner.
+     * Local experiment: exposes command-only APIs (e.g. reload()) to lifecycle handlers.
+     */
+    createCommandContext(): ExtensionCommandContext;
     /**
      * Register or override a model provider.
      *
@@ -1206,6 +1214,8 @@ export interface ExtensionActions {
     setModel: SetModelHandler;
     getThinkingLevel: GetThinkingLevelHandler;
     setThinkingLevel: SetThinkingLevelHandler;
+    /** Create a fresh ExtensionCommandContext bound to the current runner. Local experiment. */
+    createCommandContext: () => ExtensionCommandContext;
 }
 /**
  * Actions for ExtensionContext (ctx.* in event handlers).
@@ -1221,6 +1231,7 @@ export interface ExtensionContextActions {
     hasPendingMessages: () => boolean;
     shutdown: () => void;
     getContextUsage: () => ContextUsage | undefined;
+    getSettingsManager: () => SettingsManager;
     compact: (options?: CompactOptions) => void;
     getSystemPrompt: () => string;
     getSystemPromptOptions?: () => BuildSystemPromptOptions;
diff --git a/dist/core/session-manager.d.ts b/dist/core/session-manager.d.ts
index 8cdfbde..ad31576 100644
--- a/dist/core/session-manager.d.ts
+++ b/dist/core/session-manager.d.ts
@@ -13,6 +13,10 @@ export interface SessionHeader {
 export interface NewSessionOptions {
     id?: string;
     parentSession?: string;
+    /** pi-c2 patch: fixed filename for the session transcript (e.g. "transcript.jsonl"). */
+    sessionFilename?: string;
+    /** pi-c2 patch: private storage boundary whose existing ancestors are repaired to 0700. */
+    privateRoot?: string;
 }
 export interface SessionEntryBase {
     type: string;
diff --git a/dist/core/session-manager.js b/dist/core/session-manager.js
index 92e8070..13a5e22 100644
--- a/dist/core/session-manager.js
+++ b/dist/core/session-manager.js
@@ -1,14 +1,34 @@
 import { uuidv7 } from "@earendil-works/pi-ai";
 import { randomUUID } from "crypto";
-import { appendFileSync, closeSync, createReadStream, existsSync, mkdirSync, openSync, readdirSync, readSync, statSync, writeFileSync, } from "fs";
+import { appendFileSync, chmodSync, closeSync, createReadStream, existsSync, mkdirSync, openSync, readdirSync, readSync, statSync, writeFileSync, } from "fs";
 import { readdir, stat } from "fs/promises";
-import { join, resolve } from "path";
+import { dirname, join, resolve } from "path";
 import { createInterface } from "readline";
 import { StringDecoder } from "string_decoder";
 import { APP_NAME, getAgentDir as getDefaultAgentDir, getSessionsDir } from "../config.js";
 import { normalizePath, resolvePath } from "../utils/paths.js";
 import { createBranchSummaryMessage, createCompactionSummaryMessage, createCustomMessage, } from "./messages.js";
 export const CURRENT_SESSION_VERSION = 3;
+
+/** pi-c2 patch: enforce owner-only modes on the session directory and file. */
+function ensurePrivateSessionDir(dir, boundary) {
+    if (!dir) return;
+    mkdirSync(dir, { recursive: true, mode: 0o700 });
+    const resolved = resolve(dir);
+    const stop = boundary ? resolve(boundary) : resolved;
+    for (let current = resolved; ; ) {
+        chmodSync(current, 0o700);
+        if (current === stop) break;
+        const parent = dirname(current);
+        if (parent === current) break;
+        current = parent;
+    }
+}
+function ensurePrivateSessionFile(file) {
+    if (!file) return;
+    try { chmodSync(file, 0o600); } catch { /* best-effort */ }
+}
+
 function createSessionId() {
     return uuidv7();
 }
@@ -597,8 +617,8 @@ export class SessionManager {
         this.cwd = resolvePath(cwd);
         this.sessionDir = normalizePath(sessionDir);
         this.persist = persist;
-        if (persist && this.sessionDir && !existsSync(this.sessionDir)) {
-            mkdirSync(this.sessionDir, { recursive: true });
+        if (persist && this.sessionDir) {
+            ensurePrivateSessionDir(this.sessionDir, newSessionOptions?.privateRoot);
         }
         if (sessionFile) {
             this._setSessionFile(sessionFile, preloadedFileEntries);
@@ -613,6 +633,10 @@ export class SessionManager {
     }
     _setSessionFile(sessionFile, preloadedFileEntries) {
         this.sessionFile = resolvePath(sessionFile);
+        if (this.persist) {
+            ensurePrivateSessionDir(this.getSessionDir(), undefined);
+            ensurePrivateSessionFile(this.sessionFile);
+        }
         if (existsSync(this.sessionFile)) {
             this.fileEntries = preloadedFileEntries ?? loadEntriesFromFile(this.sessionFile);
             // If file was empty, initialize it with a valid session header. If it was
@@ -663,11 +687,19 @@ export class SessionManager {
         this.leafId = null;
         this.flushed = false;
         if (this.persist) {
-            const fileTimestamp = timestamp.replace(/[:.]/g, "-");
-            this.sessionFile = join(this.getSessionDir(), `${fileTimestamp}_${this.sessionId}.jsonl`);
+            ensurePrivateSessionDir(this.getSessionDir(), undefined);
+            this.sessionFile = join(this.getSessionDir(), this._resolveSessionFilename(options));
         }
         return this.sessionFile;
     }
+    /** pi-c2 patch: prefer a caller-fixed sessionFilename, else the stock timestamped name. */
+    _resolveSessionFilename(options) {
+        const explicit = options?.sessionFilename;
+        if (typeof explicit === "string" && explicit.length > 0) return explicit;
+        const timestamp = new Date().toISOString();
+        const fileTimestamp = timestamp.replace(/[:.]/g, "-");
+        return `${fileTimestamp}_${this.sessionId}.jsonl`;
+    }
     _buildIndex() {
         this.byId.clear();
         this.labelsById.clear();
@@ -736,7 +768,7 @@ export class SessionManager {
             return;
         }
         if (!this.flushed) {
-            const fd = openSync(this.sessionFile, "wx");
+            const fd = openSync(this.sessionFile, existsSync(this.sessionFile) ? "w" : "wx");
             try {
                 for (const e of this.fileEntries) {
                     writeFileSync(fd, `${JSON.stringify(e)}\n`);
@@ -745,10 +777,12 @@ export class SessionManager {
             finally {
                 closeSync(fd);
             }
+            ensurePrivateSessionFile(this.sessionFile);
             this.flushed = true;
         }
         else {
             appendFileSync(this.sessionFile, `${JSON.stringify(entry)}\n`);
+            ensurePrivateSessionFile(this.sessionFile);
         }
     }
     _appendEntry(entry) {
diff --git a/dist/core/settings-manager.d.ts b/dist/core/settings-manager.d.ts
index 494157a..9763ae4 100644
--- a/dist/core/settings-manager.d.ts
+++ b/dist/core/settings-manager.d.ts
@@ -5,6 +5,8 @@ export interface CompactionSettings {
     enabled?: boolean;
     reserveTokens?: number;
     keepRecentTokens?: number;
+    /** pi-c2 patch: percentage of the context window that triggers compaction. */
+    thresholdPercent?: number;
 }
 export interface BranchSummarySettings {
     reserveTokens?: number;
@@ -205,12 +207,15 @@ export declare class SettingsManager {
     setTransport(transport: TransportSetting): void;
     getCompactionEnabled(): boolean;
     setCompactionEnabled(enabled: boolean): void;
+    setCompactionThresholdPercent(percent: number): void;
+    getCompactionThresholdPercent(): number | undefined;
     getCompactionReserveTokens(): number;
     getCompactionKeepRecentTokens(): number;
     getCompactionSettings(): {
         enabled: boolean;
         reserveTokens: number;
         keepRecentTokens: number;
+        thresholdPercent?: number;
     };
     getBranchSummarySettings(): {
         reserveTokens: number;
diff --git a/dist/core/settings-manager.js b/dist/core/settings-manager.js
index d7fc89c..72ad0b1 100644
--- a/dist/core/settings-manager.js
+++ b/dist/core/settings-manager.js
@@ -129,6 +129,8 @@ export class SettingsManager {
     globalSettingsLoadError = null; // Track if global settings file had parse errors
     projectSettingsLoadError = null; // Track if project settings file had parse errors
     writeQueue = Promise.resolve();
+    /** pi-c2 patch: in-memory percentage threshold that survives save() re-merges. */
+    thresholdPercentOverride;
     errors;
     constructor(storage, initialGlobal, initialProject, globalLoadError = null, projectLoadError = null, initialErrors = [], projectTrusted = true) {
         this.storage = storage;
@@ -520,11 +522,23 @@ export class SettingsManager {
     getCompactionKeepRecentTokens() {
         return this.settings.compaction?.keepRecentTokens ?? 20000;
     }
+    /** pi-c2 patch: set the percentage-based compaction threshold (in-memory only). */
+    setCompactionThresholdPercent(percent) {
+        this.thresholdPercentOverride = percent;
+    }
+    /** pi-c2 patch: the effective percentage threshold, when configured. */
+    getCompactionThresholdPercent() {
+        if (typeof this.thresholdPercentOverride === "number") {
+            return this.thresholdPercentOverride;
+        }
+        return this.settings.compaction?.thresholdPercent;
+    }
     getCompactionSettings() {
         return {
             enabled: this.getCompactionEnabled(),
             reserveTokens: this.getCompactionReserveTokens(),
             keepRecentTokens: this.getCompactionKeepRecentTokens(),
+            thresholdPercent: this.getCompactionThresholdPercent(),
         };
     }
     getBranchSummarySettings() {
