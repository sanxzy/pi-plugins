{
  "name": "@xzy-ai/core",
  "version": "0.1.0",
  "description": "pi-code core: pure domain models, job lifecycle rules, control rules, and shared constants",
  "type": "module",
  "main": "./src/index.ts",
  "private": true,
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "node --test --import tsx tests/job-session.test.ts tests/child-session-live.test.ts tests/goal.test.ts tests/references.test.ts"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.22.2"
  }
}