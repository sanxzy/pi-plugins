{
  "name": "@xzy-ai/observability",
  "version": "0.1.0",
  "description": "Scoped, masked, durable pi-code observability",
  "type": "module",
  "main": "./src/index.ts",
  "private": true,
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "node --test --import ../../test-setup.mjs --import tsx tests/observability.test.ts"
  },
  "dependencies": {
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "tsx": "^4.22.2"
  }
}
