import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Register the question tool. */
export function registerQuestionTool(_pi: ExtensionAPI): void {
  throw new Error("not implemented");
}
