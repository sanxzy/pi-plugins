{
  "name": "@xzy-ai/plugins",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "pnpm -r --if-present run typecheck",
    "test": "pnpm -r --if-present run test",
    "test:all": "bash scripts/test-all.sh",
    "build": "pnpm run typecheck && pnpm run build:pi-c2",
    "build:pi-c2": "node packages/pi-c2/scripts/build.mjs"
  },
  "engines": {
    "node": ">=22.19.0",
    "pnpm": ">=11.5.0"
  },
  "packageManager": "pnpm@11.5.0",
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
